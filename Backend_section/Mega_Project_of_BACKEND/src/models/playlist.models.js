/**
_id string pk
name string
owner ObjectId user
videos ObjectId videos
createdAt Date
updatedAt Date
 */

import { mongoose, Schema } from "mongoose";

const playlistSchema = new Schema(
    {
        
    }
);
